apiVersion: v1
kind: Secret
metadata:
  name: crystal-code-quest-secrets
  namespace: crystal-code-quest
type: Opaque
stringData:
  # Replace this with a strong, randomly generated 32-byte hex secret.
  # Example: openssl rand -hex 32
  SESSION_SECRET: "REPLACE_WITH_A_32_BYTE_HEX_SECRET"
